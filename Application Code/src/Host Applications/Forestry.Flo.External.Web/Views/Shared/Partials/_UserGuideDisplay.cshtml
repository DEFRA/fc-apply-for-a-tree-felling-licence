@using Forestry.Flo.External.Web.Infrastructure

@{
    if (TempData.ContainsKey(ControllerExtensions.UserGuideKey))
    {
        <div class="govuk-notification-banner" role="region" aria-labelledby="govuk-notification-banner-title" data-module="govuk-notification-banner">
            <div class="govuk-notification-banner__header">
                <h2 class="govuk-notification-banner__title" id="govuk-notification-banner-title">
                    Getting Started
                </h2>
            </div>
            <div class="govuk-notification-banner__content">
                <p class="govuk-notification-banner__heading">
                    @TempData[ControllerExtensions.UserGuideKey]

                    @if (TempData.ContainsKey(ControllerExtensions.UserGuideLinkKey))
                    {
                        var link = TempData.Get<ControllerExtensions.NotificationLink>(ControllerExtensions.UserGuideLinkKey);
                        
                        <a class="govuk-notification-banner__link" href="@link.Link">@link.Text</a>@Html.Raw(".")
                    }
                </p>
            </div>
        </div>
    }
}