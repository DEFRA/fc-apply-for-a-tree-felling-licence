@using Forestry.Flo.External.Web.Infrastructure
@using Forestry.Flo.External.Web.Models.UserAccount.AccountTypeViewModels
@model Forestry.Flo.External.Web.Models.UserAccount.AccountTypeViewModels.TrustTypeViewModel

@{
    ViewData["Title"] = "What type of Trust are you?";
    ViewData.ShowBackLink();
}

<div class="govuk-grid-column-two-thirds">

    <form method="post">

        <govuk-form-group asp-for="OrganisationStatus">

            <fieldset class="govuk-fieldset">

                <legend class="govuk-fieldset__legend govuk-fieldset__legend--l">
                    <h1 class="govuk-fieldset__heading">
                        @ViewData["Title"]
                    </h1>
                </legend>

                <validation asp-for="OrganisationStatus"></validation>

                <div class="govuk-radios" data-module="govuk-radios">

                    <div class="govuk-radios__item">

                        @Html.RadioButtonFor(x => x.OrganisationStatus, OrganisationStatus.Individual, new { @class = "govuk-radios__input" })

                        <label class="govuk-label govuk-radios__label" asp-for="OrganisationStatus">
                            Individual trustee
                        </label>

                    </div>

                    <div class="govuk-radios__item">

                        @Html.RadioButtonFor(x => x.OrganisationStatus, OrganisationStatus.Organisation, new { @class = "govuk-radios__input", @id = "organisation-trust" })

                        <label class="govuk-label govuk-radios__label" for="organisation-trust">
                            Organisational representative
                        </label>

                    </div>

                </div>
            </fieldset>

        </govuk-form-group>

        <div class="govuk-form-group">

            <div class="govuk-button-group">

                <button data-prevent-double-click="true" type="submit" class="govuk-button" data-module="govuk-button">
                    Continue
                </button>

            </div>

        </div>


    </form>

</div>